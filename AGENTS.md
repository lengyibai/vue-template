# AI 开发规范

> 本文件是项目规范总入口。详细规则只在 `agents` 目录维护，避免多处重复导致规则漂移。

## 规则优先级与冲突处理

规则优先级固定为：

1. 语义正确性（功能与交互）
2. 业务约束
3. 框架与工程约定（Vue、TypeScript、CSS）
4. 注释与排版风格

- 若规则冲突，按以上优先级执行；不能确定时先保留现有正确行为，再向用户确认。
- `wzry-main` 仅作为代码组织与注释风格样本，不继承其中的 snake_case、组件标签大小写不统一、对象方法简写等历史写法。
- 修改现有文件时，只优化本次改动涉及或紧邻的代码；不得借规范之名扩大范围、重写无关模块。

## 执行限制

- 不要执行 `package.json` 中定义的任何脚本命令。
- 除非用户明确授权，否则禁止运行任何测试命令，包括单元测试、集成测试、端到端测试、覆盖率命令，以及 `npm test`、`pnpm test`、`vitest`、`jest`、`cypress`、`playwright test` 等相关命令。
- 执行代码前先读取 `BUSINESS.md`；若文件存在，必须理解其中的业务约束后再继续。
- 每次编辑文件前都要重新读取该文件。若内容相较上次读取已变化，必须基于最新内容继续修改，不得还原或覆盖用户改动。
- 用户可见的提示文案默认使用简体中文；设计稿明确指定其他语言时，以设计稿为准。
- 除非用户明确要求，否则不要打开本地 URL、调用浏览器演示能力或主动弹出可视化试用提示。
- 遵循项目现有 ESLint、Prettier、Stylelint 和 TypeScript 配置，但不得为验证规范而执行会改写文件的命令。

## 分类规范索引

根据改动内容，在编辑前读取对应规范：

| 改动类型 | 必读规范 |
| --- | --- |
| 注释、JSDoc、Template 模块说明 | [注释规范](./agents/注释规范.md) |
| TypeScript、类型、函数、Pinia、静态资源 | [TypeScript 规范](./agents/TypeScript规范.md) |
| HTML、Vue Template 页面结构 | [HTML 规范](./agents/HTML规范.md) |
| Vue 页面与组件 | [Vue 规范](./agents/Vue规范.md)、[HTML 规范](./agents/HTML规范.md)、[Vue 文件代码排版](./agents/Vue文件代码排版.md)、[TypeScript 规范](./agents/TypeScript规范.md)、[注释规范](./agents/注释规范.md)、[CSS 规范](./agents/CSS规范.md) |
| Less、CSS、动画 | [CSS 规范](./agents/CSS规范.md) |
| 常量、列表、配置与默认数据 | [数据排版](./agents/数据排版.md) |

- 一次改动涉及多种文件或职责时，相关分类规范都必须读取。
- 分类文件是详细规则的唯一事实来源；不要在本文件重复维护具体格式规则。
